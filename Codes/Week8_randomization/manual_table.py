trial_condition = [
    (1, 'old'),
    (1, 'new'),
    (2, 'old'),
    (2, 'new')
]

numpy.random.shuffle(trial_condition)

trial_condition = range(12)
numpy.random.shuffle(trial_condition)

getRealCondition(trial_condition)


for i in range(6):
    something
